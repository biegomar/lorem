export function words(numberOfWords: number): string {
    let result: string = "";

    for (let index = 0; index < numberOfWords; index++) {
        result += "lorem ";
        
    }

    return result;
  }